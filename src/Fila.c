/*	Name: Fila.h 		Author: Eduardo Cruz de Souza	Date: 03/05/2022	Description: Implementation of Queue data structure*/#include <stdio.h>#include <stdbool.h>#include "Fila.h"Fila createQueue(){    Fila A;    A.inicio = sinal;    A.fim = sinal;    return A;}bool emptyQueue(Fila A){    bool vazia = true;    int i = A.inicio;    if(i != sinal) vazia = false;    return vazia;}unsigned char getFirst(Fila A){    unsigned char x, y;    if(A.inicio == sinal) x = ghost;    else{        y = A.inicio;        x = A.Vetor[y];    }    return x;}Fila pushQueue(Fila A, unsigned char y){    int seg;    seg = (A.fim + 1) % Max;    if(seg != A.inicio){        A.Vetor[seg] = y;        if(A.inicio ==  sinal) A.inicio = seg;        A.fim = seg;    }    return A;}Fila popQueue(Fila A){    int seg;    if(A.inicio != sinal){        if(A.inicio == A.fim){            A.inicio = sinal;            A.fim = sinal;        }else{            seg = (A.inicio + 1) % Max;            A.inicio = seg;        }    }    return A;}void mostrarQueue(Fila A){	int i, f;	i = A.inicio;	f = A.fim;	int k;		if(A.inicio == sinal) printf("Fila Vazia!!");	else {		do{			k = (i) % Max;			printf("%d\n", A.Vetor[k]);			i++;					} while (i != f + 1);	}}